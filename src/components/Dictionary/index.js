import DictionarySelect from './dictionary-select.vue'

/* istanbulistanbul ignore next */
DictionarySelect.install = function(Vue) {
  Vue.component(DictionarySelect.name, DictionarySelect)
}

export default DictionarySelect
